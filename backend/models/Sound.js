const mongoose = require('mongoose'); const soundSchema = new mongoose.Schema({ name: { type: String, required: true }, file: { type: String, required: true } }, { timestamps: true }); module.exports = mongoose.model('Sound', soundSchema);