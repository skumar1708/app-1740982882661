const Sound = require('../models/Sound'); exports.getSounds = async (req, res) => { try { const sounds = await Sound.find(); res.json(sounds); } catch (error) { res.status(500).json({ message: error.message }); } }; exports.uploadSound = async (req, res) => { const { name, file } = req.body; try { const newSound = new Sound({ name, file }); await newSound.save(); res.status(201).json(newSound); } catch (error) { res.status(500).json({ message: error.message }); } };